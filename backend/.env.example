API_HOST=0.0.0.0
PORT=8080
DEBUG=false

# Gemini 2.5 Pro API key (get free at https://aistudio.google.com/apikey)
# Set this to enable AI-powered reasoning in the MCP agent and multi-agent pipeline
# Without it, the system uses built-in analytical models (all features still work)
GEMINI_API_KEY=

# Optional: PostgreSQL instead of SQLite (not needed for Cloud Run single-instance)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/pandemic_twin
